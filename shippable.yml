language: python

python:
  - 2.7

build:
  pre_ci:
    - docker pull ubuntu:14.04
    - docker build --rm -t therickys93/ubuntu14 .

  pre_ci_boot:
    image_name: therickys93/ubuntu14
    image_tag: latest
    pull: false

  ci:
    - bash /ubuntu/test/test.sh
